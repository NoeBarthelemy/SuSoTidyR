% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/values_to_labels.R
\name{values_to_labels}
\alias{values_to_labels}
\title{Remplacer des valeurs par leurs labels dans un data frame / Replace Values with Labels in a Data Frame}
\usage{
values_to_labels(data, nomenclature_df, columns = "all")
}
\arguments{
\item{data}{\strong{Français :} Le data frame contenant les données avec les valeurs codées (par exemple, \code{ESS_2024}).
\strong{English :} The data frame containing the coded values (e.g., \code{ESS_2024}).}

\item{nomenclature_df}{\strong{Français :} Le data frame de nomenclature contenant les correspondances entre les variables, les valeurs
et les labels (doit contenir au moins les colonnes \code{variable_name}, \code{value}, \code{value_label}).
\strong{English :} The nomenclature data frame containing mappings between variables, values, and labels
(must include at least \code{variable_name}, \code{value}, and \code{value_label} columns).}

\item{columns}{\strong{Français :} Vecteur de noms de colonnes à traiter. Peut être \code{"all"} pour toutes les colonnes, ou un
vecteur de noms spécifiques (par exemple, \code{c("Colonne1", "Colonne2")}).
\strong{English :} Vector of column names to process. Can be \code{"all"} for all columns, or a specific vector
of column names (e.g., \code{c("Column1", "Column2")}).}
}
\value{
\strong{Français :} Le data frame avec les valeurs remplacées par les labels, si disponible dans la nomenclature.
Un message est affiché indiquant les colonnes pour lesquelles les valeurs ont été remplacées et celles pour
lesquelles aucun remplacement n'a été fait.
\strong{English :} The data frame with values replaced by labels if available in the nomenclature.
A message is displayed showing which columns had values replaced and which did not.
}
\description{
\strong{Français :} Cette fonction remplace les valeurs codées par leurs labels respectifs, en utilisant une
nomenclature fournie dans un data frame de correspondance. Elle peut être appliquée à toutes les colonnes
ou uniquement à une sélection spécifique. La fonction est compatible avec \code{dplyr}, permettant un usage
dans une chaîne de pipes.

\strong{English :} This function replaces coded values with their corresponding labels, using a lookup
(nomenclature) data frame. It can be applied to all columns or only to specific ones, as specified.
The function is compatible with \code{dplyr} for use within a pipe chain.
}
\details{
\strong{Français :} Cette fonction est conçue pour être utilisée dans une chaîne \code{dplyr} et retourne un data frame
modifié. Elle remplace les valeurs dans \code{data} par les labels présents dans \code{nomenclature_df} pour les colonnes
spécifiées. Si \code{columns} est \code{"all"}, toutes les colonnes de \code{data} seront traitées, sinon seules les colonnes
spécifiées dans \code{columns} seront modifiées.

\strong{English :} This function is designed to be used within a \code{dplyr} pipeline and returns a modified data frame.
It replaces values in \code{data} with labels from \code{nomenclature_df} for the specified columns. If \code{columns}
is \code{"all"}, all columns in \code{data} are processed; otherwise, only the columns specified in \code{columns} are modified.
}
\examples{
\dontrun{
# Exemple d'utilisation en français
ESS_2024_labeled <- ESS_2024 \%>\%
  values_to_labels(nomenclature_df, columns = "all")

# Example in English
ESS_2024_labeled <- ESS_2024 \%>\%
  values_to_labels(nomenclature_df, columns = "all")
}

}
